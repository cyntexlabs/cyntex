# Cyntex server external configuration.
#
# Files in this directory override the defaults baked into the fat-jar; the launcher adds this
# directory to the Spring configuration search path. It is intentionally empty at L1 — connection
# settings (e.g. MongoDB) land in later tasks. It ships present so the distribution always carries
# the bin/ conf/ lib/ layout.
