/**
 * Transform execution port: the in-pipeline transform contract.
 *
 * <p>Placeholder package reserving the port; the transform contract is filled in when the
 * transform runtime lands. Rule R2: this module depends only on the core ring.
 */
package io.cyntex.spi.transform;
