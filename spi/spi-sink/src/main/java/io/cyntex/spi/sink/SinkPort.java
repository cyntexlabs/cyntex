package io.cyntex.spi.sink;

/**
 * Write-side sink port (delivery to a target).
 *
 * <p>Placeholder marker reserving the port name; no methods are defined yet. The sink
 * contract is added when the write-side runtime lands. Rule R2: ports depend on the core
 * ring only.
 */
public interface SinkPort {
}
