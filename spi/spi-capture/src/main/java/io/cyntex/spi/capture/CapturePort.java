package io.cyntex.spi.capture;

/**
 * Read-side capture port (batch / CDC extraction).
 *
 * <p>Placeholder marker reserving the port name; no methods are defined yet. The capture
 * contract is added when the read-side runtime lands. Rule R2: ports depend on the core
 * ring only.
 */
public interface CapturePort {
}
