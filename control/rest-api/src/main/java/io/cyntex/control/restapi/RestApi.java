package io.cyntex.control.restapi;

/**
 * REST API entry point: the HTTP presentation adapter over the control core.
 *
 * <p>Placeholder reserving the module; not instantiable and carries no behavior yet. The HTTP
 * endpoints are implemented here later. Rule R5: depends on control-core + core only.
 */
public final class RestApi {

    private RestApi() {
    }
}
