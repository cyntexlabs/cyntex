package io.cyntex.adapters.pdk;

/**
 * PDK runtime bridge entry point.
 *
 * <p>Placeholder reserving the module; not instantiable and carries no behavior yet. The PDK
 * bridge (capture + sink, backed by the connector ecosystem) is implemented here later. Rule
 * R3: this is the only module permitted to import the PDK API.
 */
public final class PdkAdapter {

    private PdkAdapter() {
    }
}
