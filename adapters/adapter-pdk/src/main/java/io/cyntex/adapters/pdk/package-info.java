/**
 * PDK runtime bridge: the capture + sink adapter backed by the PDK connector ecosystem.
 *
 * <p>Placeholder package reserving the module. This is the only place allowed to import the
 * PDK API (rule R3); the PDK dependency and the bridge implementation are added when the
 * bridge lands.
 */
package io.cyntex.adapters.pdk;
